#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#pragma pack(1)//For not wasting memory
//because when we define a structure if it s 32 bit
//the rooms are just 4 bytes and if the structer is like
//inta char b int c 
//there would be 12 byte

int main(void) {
  

  return 0;
}
